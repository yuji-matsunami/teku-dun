# M0-I1 Android バックグラウンド位置・Health Connect 収集の検証

対応 Issue: [#5](https://github.com/yuji-matsunami/teku-dun/issues/5)

## 目的

Android 実機で、**画面ロック中も**位置情報と歩数を取得できるかを確認する。

あわせて [技術選定](../tech-stack.md) の未決事項である
「`tracelet` が `flutter_background_geolocation` の代替として実用に足るか」に判断材料を与える。

## 検証しないこと

iOS、24時間計測、本番向け最適化、Drift への保存、API 送信。

## 成果物

| 成果物 | 場所 |
| --- | --- |
| 検証用アプリ | 本リポジトリには含めていない（下記「検証用アプリについて」を参照） |
| 実行手順 | 本書「手順」 |
| 比較表 | 本書「比較結果」 |

---

## 検証用アプリについて

検証のために、2つのライブラリを同一端末・同一設定・同一ログ形式で比較できる
使い捨てのアプリ（Flutter / Android のみ、約2,900行）を作成した。

**このアプリはリポジトリに含めていない。** 検証が終われば役目を終えるものであり、
`flutter create` が生成する雛形を含めると8,000行を超えてレビューの負担になるためである。
コードは作成者の手元に残してあり、必要になった時点で共有できる。

以降の「設計」「手順」の節は、**どのように検証したかを追えるようにするための記録**である。
再実施が必要になった場合は、アプリを共有したうえでこの手順に従う。

## 検証アプリの設計

同一アプリ内でバックグラウンド位置ライブラリを切り替えられるようにし、
**両候補を同一端末・同一フォーマットのログで比較できる**ようにする。

```
[ UI ]  プロバイダ選択 / 権限 / セッション開始・停止 / 集計表示 / エクスポート
   |
[ LocationProvider (抽象) ]
   |-- FbgLocationProvider      : flutter_background_geolocation
   |-- TraceletLocationProvider : tracelet
   |
   v
[ LocationSample (共通スキーマ) ] --> [ JsonlLogSink ] --> sessions/<id>/events.jsonl
                                                            + meta.json
   ^
[ HealthStepsReader ] Health Connect の歩数
[ BatterySampler ]    電池残量
```

比較を成立させるうえで重要な点は次の3つ。

1. **共通スキーマに正規化する。** 各ライブラリの位置オブジェクトはフィールド名も単位も異なる。
   受け取った直後に `LocationSample` へ変換し、以降は同じ経路で扱う。
2. **1行1レコードの JSONL へ即時 flush する。** 散歩中に OS がアプリを停止させても、
   そこまでのログが残る。まとめ書きにすると欠損の有無自体が測れない。
3. **集計はアプリ内で行う。** 散歩から戻った直後に取得間隔・欠損・電池消費が読めれば、
   比較表をその場で埋められる。

---


## 予備検証（bg_test）で判明したこと

本番の比較計測に入る前に、`flutter_background_geolocation` 単体が実機で動くかを
別アプリ（`bg_test`）で確認した。以下はそのときに得られた事実である。

### 実施条件

| 項目 | 値 |
| --- | --- |
| 端末 | Samsung SC-51D |
| Android | 16 |
| 実施日 | 2026-08-22 |
| ライブラリ | flutter_background_geolocation 5.5.0（DEBUGビルド） |
| 設定 | `distanceFilter: 0`、`stopOnTerminate: false`、`startOnBoot: true` |

### 判明したこと

| 項目 | 結果 |
| --- | --- |
| 位置情報の取得 | 成立。精度 4〜8m |
| 取得間隔 | 約1秒 |
| 位置情報の権限 | **「常に許可」が必須。** 「アプリの使用中のみ」ではバックグラウンドで取得できない |
| モーション判定 | 動作（`DetectedActivity [type=STILL, confidence=100]`、isMoving の遷移を確認） |
| アプリ終了後のネイティブ側 | **記録を継続**（`MainActivity was destroyed` / `stopOnTerminate: false` / `enabled: true`） |
| DEBUGビルドのライセンス | 不要。警告なく全機能が動作 |

### 取得間隔が約1秒になった理由

`distanceFilter: 0` にしたためである。fbg は `distanceFilter` が
`locationUpdateInterval` を上書きする仕様で、0 にして初めて時間駆動になる。

**本検証アプリは両ライブラリを揃えるため `distanceFilter: 10m` にしてあるので、
fbg は移動距離駆動になり、この1秒間隔にはならない。** 比較表を読むときに
この違いを取り違えないこと。

### `enableHeadless: true` だけでは動かない

`bg_test` では `enableHeadless: true` を設定していたが、
`registerHeadlessTask()` を呼んでいなかった。その結果、ログには

```
☯️  HeadlessMode? true
💀 [HeadlessTask terminate]
🔴  Cleared callbacks
```

と出るものの、**アプリ終了後は Dart 側で一切何も記録されなかった。**
設定とコールバック登録の両方が必要である。本検証アプリでは
`main()` で両ライブラリのヘッドレスタスクを登録している。

### ログの取り方に関する注意

`adb logcat | grep flutter` は **Dart 側の出力しか拾わない。**
アプリが終了するとネイティブが記録を続けていても何も出なくなるため、
これだけを見て「計測が止まった」と判断してはいけない。

- ネイティブの動作を見る: `adb logcat | grep TSLocationManager`
- 確実なのは、本検証アプリのセッション詳細画面で
  「プラグイン内部記録との突き合わせ」と「ヘッドレス記録件数」を確認すること

---
## 手順

### 前提

| 項目 | 条件 |
| --- | --- |
| 端末 | Android 8.0 (API 26) 以上の実機。エミュレータでは意味のある結果が出ない |
| Health Connect | Android 14 未満は Play ストアからのインストールが必要。アプリ内に導線がある |
| 歩数の供給元 | Health Connect 自体は歩数を作らない。Fitbit、Google Fit、端末標準の健康アプリなど、歩数を書き込むアプリが1つ必要 |
| ビルド | DEBUG ビルドのみ。`flutter_background_geolocation` は DEBUG ならライセンス不要 |
| 開発環境 | Flutter 3.47.1 / Dart 3.13.1 で確認済み |

### 1. アプリを端末へ入れる

```
cd <検証アプリのディレクトリ>
flutter pub get
flutter run            # 端末を接続した状態で
```

APK だけ作る場合は次を実行し、`build/app/outputs/flutter-apk/app-debug.apk` を端末へ転送する。

```
flutter build apk --debug
```

### 2. 権限を許可する

アプリの「権限をリクエスト」を押し、順に許可する。

1. 位置情報 — **「正確な位置情報」かつ「常に許可」** を選ぶ
2. 身体活動
3. 通知
4. Health Connect の歩数読み取り

Android では「常に許可」を最初のダイアログで選べない。まず「アプリの使用中のみ許可」を選び、
そのあとに再度出るダイアログ、または設定画面から「常に許可」へ格上げする必要がある。
アプリはこの順序でリクエストするようになっている。

### 3. 電池最適化を外す

端末の設定から、本アプリを電池最適化の対象外にする。

```
設定 > アプリ > android_sensing_spike > バッテリー > 制限なし
```

メーカー独自の省電力機能（Samsung のスリープ中のアプリ、Xiaomi の自動起動管理など）も
併せて解除する。**ここを外さないと、計測が止まったのが OS の制限によるものか
ライブラリの問題かを切り分けられない。**

### 4. 1回目の計測（flutter_background_geolocation）

1. プロバイダで `flutter_background_geolocation` を選ぶ
2. **充電ケーブルを抜く**（挿したままだと電池消費を比較できない）
3. 開始ボタンを押す
4. 画面をロックし、端末をポケットへ入れる
5. **10〜15分歩く**
6. 戻ったら端末を取り出し、停止ボタンを押す

歩行中に画面を点けて確認する必要はない。むしろ画面を点けると、
「画面ロック中も動くか」という検証条件が崩れる。

### 5. 2回目の計測（tracelet）

同じ経路を、同じ条件でもう一度歩く。プロバイダだけ `tracelet` へ切り替える。

比較を成立させるため、次を揃える。

- 同じ端末
- 同じ経路と歩行速度
- 同じポケット位置（体のどちら側か、上着か下か）
- 開始時の電池残量が近いこと
- 天候と時間帯が大きく違わないこと

アプリ側の計測設定（精度 high、distanceFilter 10m、更新間隔 5000ms、
`stopOnTerminate: false`、`startOnBoot: false`）は両プロバイダで同一にしてある。

### 6. 結果を読む

「セッション一覧」から各セッションを開くと、集計が表示される。

| 表示 | 意味 |
| --- | --- |
| 件数 | 取得できた位置サンプル数 |
| 取得間隔（中央値 / p95 / 最大） | 最大値がそのまま**最大欠損時間**になる |
| 欠損区間 | 60秒を超えて位置が取れなかった区間の一覧 |
| 欠損率 | 計測時間のうち欠損が占める割合 |
| 移動距離 | 精度50m超のサンプルを除外して積算した距離 |
| 精度（中央値 / p95） | 位置精度のメートル値 |
| 電池消費 | 開始・終了時の残量差と、1時間あたりの換算値 |
| 活動種別 | 歩行・車両などの判定内訳 |

この値をそのまま本書の「比較結果」表へ転記する。


### 「計測が死んだ」と「アプリだけ落ちた」の切り分け

両ライブラリとも `stopOnTerminate: false` で動くため、Android が Flutter の
Dart プロセスを停止させても、ネイティブ側のサービスは記録を続ける。
一方このアプリの JSONL ログは Dart 側でしか書けないため、
この場合ログだけが途切れる。

ログの見た目だけでは「ライブラリが計測を止めた」場合と区別がつかない。
取り違えると、実際には正常に動いていたライブラリを不採用にしかねない。

そこでセッション終了時に、プラグイン内部のDBの記録件数を読み出して
自前ログと突き合わせ、結果を画面に表示する。

| 突き合わせ結果 | 意味 |
| --- | --- |
| 両者がほぼ一致 | Dart 側の取りこぼしは無い。ログの欠損はそのままライブラリ側の挙動 |
| プラグイン側が大幅に多い | **アプリだけが落ち、ライブラリは動いていた。** ライブラリの問題ではない |
| 両者とも少ない | ライブラリまたは OS が計測を止めた |

なお `flutter_background_geolocation` は使い捨てサンプルを自前DBへ保存しないため、
両者の件数は元々完全には一致しない。大きく乖離しているかどうかで判断する。

### 集計値を読むときの注意

| 表示 | 注意点 |
| --- | --- |
| 最大取得間隔(サンプル間) | サンプルとサンプルの最大の空き。頭尾の欠損は含まない |
| 最大欠損 | 頭尾を含めた最大の欠損。**計測が途中で死んだ場合はここに出る** |
| 欠損の内訳 | 「開始直後」はGPSの初期補足で正常時も出る。「終了直前」は計測が止まった疑いが濃い |
| この計測区間の歩数 | ホーム画面の計測前の表示は当日累計なので混同しない |

`flutter_background_geolocation` の `locationUpdateInterval` は
`distanceFilter > 0` のとき無効になる（公式仕様）。
本アプリは両者とも `distanceFilter` 10m で揃えているため、
fbg は移動距離駆動で発火する。**取得間隔の差をそのままライブラリの性能差と
読まないこと。**

### 7. 権限拒否時の挙動を確認する

受け入れ条件に含まれるため、許可した場合だけでなく拒否した場合も確認する。

1. 設定からアプリの権限をすべて取り消す
2. アプリを再起動する
3. 位置情報を「許可しない」で拒否 → 画面の表示とログを確認する
4. 位置情報を「アプリの使用中のみ許可」に留める → 画面ロック後に取得が止まるかを確認する
5. Health Connect の歩数を拒否 → 歩数取得の失敗がどう表示されるかを確認する

いずれの場合もアプリが落ちないこと、および何が拒否されたか画面から判別できることを確認する。

### 8. ログを取り出す

セッション一覧の共有ボタンから `events.jsonl` を書き出せる。
`adb` で直接取り出す場合は次のとおり。

```
adb exec-out run-as jp.tekudun.android_sensing_spike \
  cat files/sessions/<セッションID>/events.jsonl > events.jsonl
```

### ログの形式

1行1レコードの JSONL。位置サンプルとイベントが同じ時系列に混ざって並ぶため、
「いつ権限が変わったか」「いつアプリがバックグラウンドへ回ったか」を
位置の欠損と突き合わせて読める。

```json
{"type":"location","seq":1,"providerId":"fbg","recordedAt":"...","receivedAt":"...","latitude":35.68,"longitude":139.76,"accuracy":8.5,"isMoving":true,"activity":"on_foot","activityConfidence":87,"batteryLevelPercent":92}
{"type":"event","at":"...","kind":"healthSnapshot","totalSteps":1234,"sourceApps":["..."]}
{"type":"event","at":"...","kind":"appLifecycle","state":"paused"}
```

計測データそのものはリポジトリへコミットしない（`.gitignore` で `*.jsonl` を除外している）。
結果は本書の比較表へ数値として転記する。

---

## 比較結果

### 実施規模

2026-08-23 から 08-30 にかけて、以下を計測した。

| 項目 | 値 |
| --- | --- |
| 計測セッション | 24件（fbg 13 / tracelet 11） |
| 総計測時間 | 約16時間 |
| 端末 | Samsung SC-51D（Android 16 / SDK 36） |
| ビルド | DEBUG のみ。費用ゼロ |

移動手段の判別は、活動種別ではなく**区間速度の実測値**で行った。
初期のセッションには電車や車での移動が混ざっており、徒歩の比較には使えていない。

### 主結果

条件を揃えた対比較を2組実施した。**本検証の結論はこの2組に基づく。**

| 組 | 内容 | ねらい |
| --- | --- | --- |
| 08-29 | 各40分 | 電池を1%刻みで5段階以上動かし、消費率を測れるようにする |
| 08-30 | 各13分半 | 受け入れ条件が求める10〜15分に合わせ、歩数を含めて比較する |

いずれも連続した時間帯に歩行し、端末には一切触れていない。
電池残量が1分刻みで記録されており、OSによる抑制を受けていないことを確認済み。

08-30 の結果は「[Health Connect と歩数](#health-connect-と歩数08-30-の対比較)」の節に記載する。

#### 08-29 の対比較（各40分）

| 観点 | `flutter_background_geolocation` | `tracelet` |
| --- | --- | --- |
| 計測時間 | 41分17秒 | 40分02秒 |
| 移動距離 | 3.540 km | 3.459 km |
| 中央速度 | 5.3 km/h | 5.3 km/h |
| 取得サンプル数 | 340 | 328 |
| 取得間隔（中央値） | 7秒 | 7秒 |
| 取得間隔（p95） | 10秒 | 10秒 |
| 取得間隔（最大） | 18秒 | 14秒 |
| **60秒超の欠損** | **0回** | **0回** |
| **移動中の取りこぼし** | **0.0%** | **0.0%** |
| 位置精度（中央値） | 10.4m | 9.1m |
| 位置精度（p95） | 19.4m | 12.3m |
| **電池消費** | **5.5〜5.8 %/時** | **5.8〜6.0 %/時** |
| 画面ロック中の継続 | 成立 | 成立 |
| アプリ強制終了後の継続 | 成立（ヘッドレス登録が必要） | 成立（ヘッドレス登録が必要） |
| 活動種別の信頼性 | 低い（後述） | 低い（後述） |
| 導入の手間 | Maven Central 配信。追加リポジトリ不要 | 同左。`enableHeadless` 設定は無く登録のみ |

#### 電池消費の読み方

両者とも40分で残量が4%減った。1%刻みで4段階動いており、
残量が変化した時刻の間隔からも算出できる。

```
fbg       83% →(6.0分) 82 →(11.5分) 81 →(10.4分) 80 →(10.7分) 79
tracelet  79% →(5.0分) 78 →(11.0分) 77 →(11.0分) 76 →( 9.0分) 75
```

最初の1段階だけは、セッション開始前からその残量だったため区間が途中から始まっている。
そこを除いた平均は fbg 10.9分/%（**5.5%/時**）、tracelet 10.3分/%（**5.8%/時**）となる。

始点終点の差から求めた値（5.8% / 6.0%）とも一致する。
**両者の差は 0.2ポイント程度で、1%刻みの分解能から来る誤差（±1.5%/時）の内側にある。
電池消費に有意な差は認められない。**

なお計測はデバッグログを有効にしたまま行っており、実際の配信ビルドでは
これより低くなる可能性がある。**それでも 40分の連続計測で 4%、
10〜15分の散歩なら 1〜1.5% にとどまる。** 想定用途では十分に実用的な水準である。

### 参考：条件の揃っていないセッション

以下の日付はセッションIDに由来する UTC 表記である。端末は UTC+9 のため、
「08-28」の計測は実際には 08-29 朝（主結果の約1時間50分前）に行われている。

| セッション | 提供元 | 時間 | 移動中の取りこぼし | 備考 |
| --- | --- | --- | --- | --- |
| 08-27 | fbg | 12分42秒 | 0.0% | 同一周回路の対比較。電池は満充電の張り付きで測定不能 |
| 08-27 | tracelet | 12分56秒 | 0.0% | 同上 |
| 08-26 | fbg | 1時間32分 | 18.3% | 経路・時間帯が不揃い |
| 08-26 | tracelet | 41分36秒 | 0.0% | 同上 |
| 08-28 | fbg | 31分57秒 | **71.2%** | **電池最適化を外していない状態**（後述） |

### 電池最適化を外していない場合の挙動

08-28 のセッションは、アプリを電池最適化の除外対象にしないまま計測したものである。
検証中の再インストールで除外設定が失われていたことに気づかずに実施した。

結果は明確だった。

| | 除外済み（08-29） | 除外なし（08-28） |
| --- | --- | --- |
| 取得間隔（中央値） | 7秒 | 24秒 |
| 移動中の取りこぼし | 0.0% | **71.2%** |
| 欠損率（静止を含む参考値） | 0.0% | 58.8% |
| 電池残量の記録（毎分のはず） | 41分で40点／40分で41点 | 32分で**8点** |

Dart側のタイマーが止められており、アプリの処理そのものがOSに凍結されていた。
電池消費が 1.9%/時 と低く出たのは省電力が優秀だったからではなく、
**GPSがほとんど動いていなかったため**である。

**これは失敗した計測ではなく、独立した発見として扱うべきものである。**
一般の利用者は電池最適化を外さない。その状態では、
このアプリは移動時間の半分以上を取りこぼす。
技術選定が挙げた「OSの制限により歩行が正しく記録されない」というリスクが、
実測で顕在化した形になる。

なお `adb shell dumpsys deviceidle whitelist` では 08-29 の計測時点でも
本アプリは除外リストに現れなかった。**Samsung 独自の省電力制御は AOSP の
Doze ホワイトリストとは別系統であり、adb からは状態を確認できない。**
設定画面での操作が必須で、その結果は計測データの質でしか確認できない。

### 活動種別は両ライブラリとも信頼できない

**本検証で最も重要な発見。**

| 実測の中央速度 | プラグイン報告速度 | ライブラリ | 活動種別 | 判定 |
| --- | --- | --- | --- | --- |
| 62.4 km/h | 52.9 km/h | fbg | `walking`（確信度100、23分継続） | 誤り |
| 74.6 km/h | 72.1 km/h | tracelet | `in_vehicle` | 正しい |
| 46.3 km/h | 58.9 km/h | fbg | `still` | 誤り |
| 39.8 km/h | 34.8 km/h | tracelet | `still` | 誤り |

一方、**速度は両者とも正確**であった。

[技術選定](../tech-stack.md) が掲げる「活動種別・移動速度・歩数と距離の整合の3つを
組み合わせ、判定はサーバ側で行う」という方針は正しい。そのうえで、
**速度を主たる根拠とし、活動種別は補助にとどめる**必要がある。
活動種別を信じる設計にすると、電車に乗るだけでゲームが進む。

### 権限まわりの挙動

許可時の挙動に加えて、**拒否時の挙動を4ケース確認した**（08-30）。
`adb` で権限を個別に取り消し、それぞれ短いセッションを実施している。

| ケース | クラッシュ | 位置情報の記録 | 挙動 |
| --- | --- | --- | --- |
| A. 位置情報を完全拒否 | しない | されない | 計測が失敗して止まる |
| B. 「使用中のみ許可」 | しない | **両ライブラリで正反対** | 下記 |
| C. 身体活動を拒否 | しない | される | 活動種別が `still` 固定になる |
| D. Health Connect 歩数を拒否 | しない | 影響なし | 歩数が「不明」と表示される |
| E. 通知を拒否 | しない | される | フォアグラウンドサービスは動作する |

**どのケースでもアプリは落ちなかった。** また、権限が足りないまま無言で
何も記録しないという事態も起きていない（Aでは計測開始そのものが失敗する）。

E は個別に試験していないが、08-26 から 08-30 にかけての全セッションが
`POST_NOTIFICATIONS: granted=false` の状態で正常に記録できていたため、
実測データから確認済みとする。

#### B. 「使用中のみ許可」での挙動が両ライブラリで正反対

**本検証で最も重要な発見のひとつ。**

`ACCESS_BACKGROUND_LOCATION` だけを拒否し（＝「アプリの使用中のみ許可」の状態）、
画面をロックして歩いた結果である。

| | `flutter_background_geolocation` | `tracelet` |
| --- | --- | --- |
| セッション長 | 8分17秒 | 10分36秒 |
| 位置サンプル | **2件** | **70件** |
| バックグラウンド化以降のサンプル | **1件（0秒後で終了）** | **69件（623秒後まで継続）** |
| 取得間隔 | — | 7〜8秒で安定 |
| プラグインが報告した権限レベル | `status: 4`（WHEN_IN_USE） | `status: 'whenInUse'` |

**fbg は画面をロックした瞬間に停止した。** 8分間走らせて記録はゼロである。

**tracelet は停止しなかった。** ロック後も10分間、7〜8秒間隔で記録し続けた。

両者とも権限レベルは正しく認識している。**同じ状況を認識したうえで、
振る舞いが正反対である。**

##### 推定される仕組み

Android では、`foregroundServiceType="location"` のフォアグラウンドサービスが
動作中のアプリは「使用中」とみなされ、`ACCESS_BACKGROUND_LOCATION` が無くても
位置情報を取得できる。常駐通知が出ている間は利用者から見えている扱いになるためである。

tracelet はこの経路を使い、fbg は `ACCESS_BACKGROUND_LOCATION` の有無で
自ら判断して停止している、という違いに見える。
**ただしこれは推定であり、実装の確認はしていない。**

##### なぜ重要か

「常に許可」を取得するのは実務上とても難しい。

- Android は最初のダイアログで「常に許可」を選ばせない。
  利用者が自分で設定画面に入る必要がある
- Google Play ではバックグラウンド位置情報の利用に**審査申請**が必要で、
  審査期間があり、通らないこともある

**tracelet であれば「使用中のみ許可」のまま散歩アプリが成立する可能性がある。**
そうであれば、利用者を設定画面へ誘導する導線が不要になり、
Play Console の審査申請も回避できるかもしれない。
技術選定が「リリース直前に着手すると間に合わない」と警告していた申請そのものを
避けられる可能性がある。

##### 留保

- **1回の観測**（10分、1端末）にすぎない
- フォアグラウンドサービスが OS に停止させられた場合の挙動は未確認
- **通知を拒否された状態でこの経路が機能するかは未確認**
- Play のポリシー上通用するかは、実際に申請してみないと分からない

#### A. 位置情報を完全拒否したときの問題

計測は失敗して止まるため、記録されないまま歩き続ける事態は避けられている。
ただし2点の問題がある。

**エラーが生の例外文字列である。**

```
計測開始に失敗しました：PlatformException(Permission denied, null, null, null)
```

何が起きたかは分かるが、利用者に見せる文言ではない。
「位置情報の権限が必要です」という案内と、設定画面への導線が要る。

**失敗のたびに空のセッションが残る。**

`startSession()` はセッションディレクトリと `meta.json` を作ってから
`provider.start()` を呼ぶため、開始に失敗すると中身のないセッションが残る。
押した回数だけ蓄積し、記録一覧を汚す。

#### 権限の状態を起動時に確認していない

アプリは起動時に権限の状態を問い合わせておらず、
「権限をリクエスト」を押すまで**「未確認」と表示される**。
拒否されていても「拒否」とは出ない。

Android はしばらく使っていないアプリの権限を自動で取り消すため、
**利用者が何もしなくても権限が外れ、その状態が画面から分からない**ことになる。
M1 では起動時に権限状態を確認し、不足していれば警告する必要がある。

#### D. 歩数の拒否がログに残らない

権限を拒否した状態で計測したときの `healthSnapshot` イベントである。

```json
{"kind":"healthSnapshot","phase":"end","dataPointCount":0,"sourceApps":[]}
```

**`totalSteps` のキーが無く、`error` も無い。**
Health Connect は権限が無くても例外を投げず `null` を返すため、
`readSteps()` の catch 節に入らないためである。

結果として、次の2つがログ上で完全に同じ見た目になる。

- 権限が拒否されている
- 権限はあるが、その区間に歩数のデータが無い

08-26 に歩数が取得できなくなった際、原因の特定に推測を要したのはこのためである。
**M1 では読み取り前に `hasPermissions()` で確認し、権限がないことを
明示的に記録・表示する必要がある。** 歩数を進行判定に使うなら、
「権限が外れていることに気づかないまま『今日は歩いていない』と判定される」
状況は避けなければならない。

なお「権限をリクエスト」を明示的に押した経路では、拒否が正しく記録されていた。

```json
{"kind":"permissionResult","source":"healthConnect","status":"denied",
 "requestReturnedTrue":false,"hasPermissionsAfterRequest":false}
```

計測中に歩数を読む経路だけが、失敗を握りつぶしている。

#### C. 身体活動を拒否した場合

落ちず、位置情報の記録も続く。活動種別は `null` にならず `still`（確信度100）が返る。

全権限を許可した状態での足踏み（08-29）と比較すると、挙動に差は出なかった。

| | 権限 | 時間 | サンプル | 活動種別 |
| --- | --- | --- | --- | --- |
| 08-29 | 全許可 | 44秒 | 1件 | `still` |
| 08-30 | 身体活動を拒否 | 58秒 | 1件 | `still` |

ただし足踏みでは移動距離がゼロであり、どちらの条件でも静止と判断されるため、
**この比較で「影響がない」と結論づけることはできない。**
移動を伴う確認は未実施である。活動種別は両ライブラリとも信頼できないことが
既に分かっているため、実害は小さいと見ているが、確証はない。

### Health Connect と歩数（08-30 の対比較）

歩数を含む対比較を 08-30 に実施した。受け入れ条件が求める **10〜15分の歩行**に収まっている。

| 観点 | `flutter_background_geolocation` | `tracelet` |
| --- | --- | --- |
| 計測時間 | 13分33秒 | 13分24秒 |
| 移動距離 | 1.181 km | 1.176 km |
| 中央速度 | 5.3 km/h | 5.3 km/h |
| 取得サンプル数 | 115 | 111 |
| 取得間隔（中央値 / p95） | 7.0秒 / 9.6秒 | 7.2秒 / 9.3秒 |
| 最大間隔 | 19.5秒 | 16.7秒 |
| **60秒超の欠損** | **0回** | **0回** |
| 位置精度（中央値 / p95） | 10.0m / 14.4m | 9.5m / 12.6m |
| **区間の歩数** | **1,453歩** | **1,401歩** |

**歩数を取得でき、位置情報から算出した距離とも整合した。**

| | 歩幅（移動距離 ÷ 歩数） |
| --- | --- |
| fbg | 1,181m ÷ 1,453歩 = 0.81 m/歩 |
| tracelet | 1,176m ÷ 1,401歩 = 0.84 m/歩 |

成人の歩幅として妥当な範囲であり、両者の差も3%に収まる。

| 項目 | 結果 |
| --- | --- |
| 歩数を取得できたか | **できた** |
| 歩数の供給元アプリ | Health Connect 経由。端末には Samsung Health が同居 |
| 移動距離との整合 | 歩幅 0.81〜0.84 m/歩。妥当な範囲 |
| Health Connect 未インストール時の挙動 | **未検証**（Android 16 では OS 組み込みのため再現できず） |

#### 歩数は分単位で正確には切り出せない

当日累計から逆算すると、わずかにずれる。

```
fbg 開始時の当日累計      189歩（00:22:03 時点）
tracelet 開始時の当日累計 1,705歩（00:35:38 時点）
差                       1,516歩
fbg が報告した区間歩数    1,453歩   ← 63歩（約4%）の差
```

2秒の隙間で63歩は歩けない。Health Connect が歩数をまとめて書き込む単位の境界に
よるものと考えられる。**歩数を秒単位の区間で厳密に切り出すことはできない**と
理解しておく必要がある。ゲームの進行判定に使う場合、数パーセントの誤差を
許容する設計にすること。

#### アプリが停止すると区間歩数は記録されない

歩数を読むのは `stopSession()` の中だけであり、ヘッドレスタスクは位置情報しか
扱っていない。そのため**計測中にアプリが OS に停止させられると、その回の
区間歩数は記録されない**（08-24 のセッションで実際に発生。位置情報は
ヘッドレスで280件記録できたが、歩数は開始時点のものしか残らなかった）。

Health Connect 側の歩数データ自体は失われないため、セッションの時刻から
後で手動で読み取ることはできる。M1 では、ヘッドレスの `terminate` イベントで
区間歩数を記録するか、復帰時にセッションを締め直す仕組みが要る。

#### 権限は再インストールで失われる

08-26 以降のセッションで歩数が取得できていない期間がある。
検証中にアプリを再インストールした際に権限が失われ、再許可しないまま
計測したためである。**アプリを入れ直したら権限の再確認を行うこと。**

---

## 結論

### 受け入れ条件の充足状況

| 条件 | 状態 | 根拠 |
| --- | --- | --- |
| 両ライブラリを同一端末・同等条件で比較する | **達成** | 08-29 / 08-30 の対比較2組 |
| 10〜15分歩行中の時刻・緯度・経度・精度を取得できる | **達成** | 08-30（各13分半）。全項目を記録 |
| Health Connect から歩数を取得できる | **達成** | 08-30。fbg 1,453歩 / tracelet 1,401歩 |
| 権限許可・拒否時の挙動を確認する | **達成** | 08-30 に拒否時を4ケース確認 |
| 欠損・取得間隔・電池消費・制約を比較表へ記録する | **達成** | 本書「比較結果」 |

Validation（画面ロック状態で歩き、両候補のログと歩数を比較する）も達成した。

**受け入れ条件はすべて満たした。** ただし結論の確度には留保があり、
実装上の課題も複数見つかっている（後述）。

### 中心体験は成立する

**Android 実機で、画面ロック中も位置情報と歩数を記録できる。**
40分ずつの対比較と、13分半ずつの対比較の両方で、
両ライブラリとも60秒を超える欠損がゼロだった。
取得間隔は中央値7秒・p95が10秒で、両者に差はない。
歩数も Health Connect から取得でき、位置情報から算出した距離とも整合した。

**電池消費も実用水準である。** 40分の連続計測で4%、10〜15分の散歩なら1〜1.5%。
デバッグログを有効にしたままの値なので、配信ビルドではさらに下がる余地がある。

**M1 へ進めない重大課題は見つからなかった。**

### `tracelet` は `flutter_background_geolocation` の代替として実用に足る

技術選定の未決事項に対する回答。**条件を揃えた対比較で、
記録品質・取得間隔・電池消費・歩数のいずれにも有意な差は認められなかった。**
位置精度はむしろ tracelet がわずかに良い（中央値 9.1m 対 10.4m、p95 12.3m 対 19.4m）。

**$399 を支払う技術的な理由は、本検証の範囲では見つからなかった。**

さらに、権限検証で **tracelet に固有の利点**が見つかった。
「アプリの使用中のみ許可」の状態でも、tracelet は画面ロック中の記録を継続した
（fbg は即座に停止した）。これが再現するなら、

- 利用者を設定画面へ誘導して「常に許可」を取る導線が不要になる
- Google Play のバックグラウンド位置情報の審査申請を回避できる可能性がある

いずれも配信までの障害を減らす。ただし1回の観測にすぎず、
フォアグラウンドサービスが停止した場合や通知を拒否された場合の挙動は未確認である。
**採用を決める前に、この点だけは反復して確かめる価値が高い。**

ただし、これをもって「tracelet を採用する」と決めるには、なお次の留保がある。

- 対等な条件での比較は 08-29 の1組（各40分）のみ。反復していない
- 端末が Samsung 1台のみ。バックグラウンド計測の成否は OEM の省電力制御に
  大きく左右されるため、1台の結果は一般化できない
- `tracelet` は単独開発者による新しいパッケージであり、**長期の保守性と
  Android の仕様変更への追随速度は数値に表れない。** ここは $399 が
  買っているものの一部であり、実測では評価できない

**技術選定が定めた「支払いは配信直前まで先送りする」方針をそのまま維持し、
tracelet を第一候補として開発を進め、配信前に再評価するのが妥当である。**
DEBUG ビルドで開発するかぎり、いずれを選んでも費用は発生しない。

### 電池最適化の扱いが製品リスクになる

**除外設定をしていない状態では、移動時間の71.2%を取りこぼした。**
一般の利用者はこの設定を触らない。

「ゲームの続きのために散歩に出たのに、歩いた分が記録されていない」という体験は、
本アプリのコンセプトを根本から損なう。M1 では次の検討が要る。

- 初回起動時に電池最適化の除外を促す導線（`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`）
- 除外されていない状態を検出し、利用者に警告する仕組み
- 取りこぼしが起きた場合に、それを利用者へどう伝えるか

### 設計へ持ち帰るべきこと

1. **徒歩判定は速度を第一の根拠にする。** 活動種別は両ライブラリとも誤る。
2. **アプリの停止を前提に組む。** 計測中にアプリが OS に停止させられるのは例外ではない。
   ヘッドレスでの記録継続と、復帰後にセッションを締め直す仕組みを設計に含める。
3. **「常に許可」への格上げ導線が要る。**
4. **電池最適化の除外を促す導線が要る。**
5. **権限の状態を起動時に確認する。** Android は未使用のアプリの権限を自動で
   取り消す。現状は「権限をリクエスト」を押すまで状態が分からず、
   拒否されていても「未確認」と表示される。
6. **歩数は「権限なし」と「データなし」を区別する。** Health Connect は権限が
   無くても例外を投げず `null` を返すため、現状は両者を区別できない。
   読み取り前に `hasPermissions()` で確認すること。
7. **エラーは利用者に伝わる文言にする。** 現状は生の例外文字列が表示される。
   設定画面への導線も要る。
8. **計測の正しさを検証する工数を見込む。** 本検証では、アプリ実装より
   「自分たちの計測が間違っていることに気づく」作業のほうが比重が大きかった。
   M1 の位置情報まわりの見積もりには同種の余裕が必要。

### 残っている検証

| 項目 | 状態 | 備考 |
| --- | --- | --- |
| 「使用中のみ許可」での tracelet の挙動の反復確認 | 1回のみ | **採用判断に直結するため優先度が高い** |
| 身体活動を拒否した状態で移動を伴う確認 | 未実施 | 足踏みでは切り分けられなかった |
| 対比較の反復 | 2組 | M1 と並行で可 |
| 別メーカー端末 | 未実施 | 端末の確保が必要 |

受け入れ条件はすべて満たしているため、**本 Issue はクローズできる。**
上記は結論の確度を上げるためのものであり、M1 と並行して進められる。
