# エージェントオーケストレーション

タスクの内容と規模に応じて、適度にサブエージェントへ委譲して進める。
ただし、1 コマンドの実行など、単純で短時間に完了する作業ではサブエージェントを利用する必要はない。
サブエージェントは、別のサブエージェントを起動したり、タスクを再委譲したりしてはならない。

## 担当モデル

- 設計、高度な思考を要するタスク、レビューは Sol に担当させる。
  - effort を設定できる場合は `medium` を使用する。
- 実装などの作業タスクは Luna に担当させる。
  - effort を設定できる場合は `max` を使用する。

## Claude 利用時の読み替え

Claude を利用する場合は、モデルの役割を次のように読み替える。

- Luna: Sonnet
- Sol: Opus

上記の読み替え後も、各モデルに割り当てるタスクの種類は同じとする。
